
(function(){
  if (!document.body.classList.contains('theme-toronto')) return;
  const shared = window.NTTCB || {};
  const esc = shared.esc || ((t)=>String(t ?? ''));
  const renderMarkdownBlock = shared.renderMarkdownBlock || ((t)=>`<p>${esc(t)}</p>`);
  const prettyDate = shared.prettyDate || ((iso)=>iso||'');
  const stars = shared.stars || (()=>'');
  function roundHalf(v){ return Math.round(v*2)/2; }
  function overall(r){ return r.overall ?? roundHalf((Number(r.cleanliness||0)+Number(r.aesthetics||0)+Number(r.facilities||0))/3); }
  function sortLatest(a,b){ return (b.date||'').localeCompare(a.date||''); }
  function sortFeatured(a,b){ if (!!b.featured !== !!a.featured) return b.featured ? 1 : -1; return (b.date||'').localeCompare(a.date||''); }
  function card(review, hrefPrefix){ const ovr=overall(review).toFixed(1); const passwordText = review.passwordRequired ? review.password : 'No'; return `<article class="review-card stack"><div class="rating-line"><div><span class="eyebrow-label">Overall</span><span class="stars">${stars(Number(ovr))}</span></div><strong class="rating-number">${ovr}</strong></div><h3>${esc(review.title)}</h3><p class="review-date">Review Date: ${prettyDate(review.date)}</p><div class="sub-ratings"><div><span>Cleanliness</span><span class="stars-wrap"><span class="stars">${stars(Number(review.cleanliness))}</span><span class="rating-mini-number">${Number(review.cleanliness).toFixed(1)}</span></span></div><div><span>Aesthetics</span><span class="stars-wrap"><span class="stars">${stars(Number(review.aesthetics))}</span><span class="rating-mini-number">${Number(review.aesthetics).toFixed(1)}</span></span></div><div><span>Facilities</span><span class="stars-wrap"><span class="stars">${stars(Number(review.facilities))}</span><span class="rating-mini-number">${Number(review.facilities).toFixed(1)}</span></span></div></div><div class="text-panel markdown-body">${renderMarkdownBlock(review.summary)}</div><div class="tag-row bottom-tags">${review.featured ? '<span class="tiny-tag featured">Featured Toilet</span>' : ''}<span class="tiny-tag">Location: ${esc(review.location)}</span><span class="tiny-tag">Area: ${esc(review.area)}</span><span class="tiny-tag">Neighborhood: ${esc(review.neighborhood)}</span><span class="tiny-tag">Accessibility: ${esc(review.accessibility)}</span><span class="tiny-tag">Suitable for: ${esc(review.suitableFor)}</span><span class="tiny-tag">Password: ${esc(passwordText)}</span></div><div class="cta-inline"><a class="mini-link" href="${hrefPrefix}${esc(review.slug)}/index.html">Read review</a></div></article>`; }
  function renderStats(reviews){ const stats=document.getElementById('torontoStats'); if(!stats) return; const avg=reviews.length ? (reviews.reduce((s,r)=>s+overall(r),0)/reviews.length).toFixed(1) : '0.0'; const neighborhoods=new Set(reviews.map((r)=>r.neighborhood)).size; const passwordFree=reviews.filter((r)=>!r.passwordRequired).length; stats.innerHTML=`<div class="stats-grid"><div class="stat-card"><div class="stat-label">Total reviews</div><div class="stat-value">${reviews.length}</div></div><div class="stat-card"><div class="stat-label">Average overall</div><div class="stat-value">${avg}</div></div><div class="stat-card"><div class="stat-label">Neighborhoods covered</div><div class="stat-value">${neighborhoods}</div></div><div class="stat-card"><div class="stat-label">Password-free spots</div><div class="stat-value">${passwordFree}</div></div></div>`; }
  function renderHome(reviews){ renderStats(reviews); const holder=document.getElementById('latestTorontoReviews'); if(holder) holder.innerHTML = reviews.slice().sort(sortLatest).slice(0,3).map((r)=>card(r,'reviews/')).join(''); }
  function renderIndex(reviews){ const holder=document.getElementById('torontoReviewIndex'); if(holder) holder.innerHTML = reviews.slice().sort(sortFeatured).map((r)=>card(r,'../')).join(''); }
  function detailSlug(){ const parts=window.location.pathname.split('/').filter(Boolean); return parts[parts.length-2]||''; }
  function renderDetail(reviews){ const hero=document.getElementById('torontoDetailHero'); const detail=document.getElementById('torontoDetailBody'); if(!hero||!detail) return; const review=reviews.find((e)=>e.slug===detailSlug()); if(!review){ hero.innerHTML='<h1>Review not found</h1><p class="lead">This review does not exist yet.</p>'; detail.innerHTML='<div class="text-panel"><p>Return to the Toronto index and try another listing.</p></div><div class="cta-inline"><a class="btn" href="../index.html">Back to all Toronto reviews</a></div>'; return; } const ovr=overall(review).toFixed(1); document.title=`${review.title} | Nihilists Take Their Coffee Black`; hero.innerHTML=`<h1>${esc(review.title)}</h1><p class="lead">Review Date: ${prettyDate(review.date)}</p>`; detail.innerHTML=`<div class="rating-line large"><div><span class="eyebrow-label">Overall</span><span class="stars">${stars(Number(ovr))}</span></div><strong class="rating-number">${ovr}</strong></div><div class="detail-grid"><div><span>Cleanliness</span><span class="stars-wrap"><span class="stars">${stars(Number(review.cleanliness))}</span><span class="rating-mini-number">${Number(review.cleanliness).toFixed(1)}</span></span></div><div><span>Aesthetics</span><span class="stars-wrap"><span class="stars">${stars(Number(review.aesthetics))}</span><span class="rating-mini-number">${Number(review.aesthetics).toFixed(1)}</span></span></div><div><span>Facilities</span><span class="stars-wrap"><span class="stars">${stars(Number(review.facilities))}</span><span class="rating-mini-number">${Number(review.facilities).toFixed(1)}</span></span></div></div><div class="text-panel markdown-body">${renderMarkdownBlock(review.summary)}</div>${review.otherNotes ? `<div class="note-panel markdown-body"><strong>Other Notes</strong>${renderMarkdownBlock(review.otherNotes)}</div>` : ''}<div class="tag-row bottom-tags">${review.featured ? '<span class="tiny-tag featured">Featured Toilet</span>' : ''}<span class="tiny-tag">Location: ${esc(review.location)}</span><span class="tiny-tag">Area: ${esc(review.area)}</span><span class="tiny-tag">Neighborhood: ${esc(review.neighborhood)}</span><span class="tiny-tag">Accessibility: ${esc(review.accessibility)}</span><span class="tiny-tag">Suitable for: ${esc(review.suitableFor)}</span><span class="tiny-tag">Password: ${esc(review.passwordRequired ? review.password : 'No')}</span></div><div class="cta-inline"><a class="btn" href="../index.html">Back to all Toronto reviews</a></div>`; }
  async function load(){ let path='../content/toronto-reviews.json'; const p=window.location.pathname; if(/\/where-to-poop-toronto\/reviews\/[^/]+\//.test(p)) path='../../../content/toronto-reviews.json'; else if(p.includes('/where-to-poop-toronto/reviews/')) path='../../content/toronto-reviews.json'; const res=await fetch(path,{cache:'no-store'}); const reviews=await res.json(); if(document.getElementById('latestTorontoReviews')) renderHome(reviews); if(document.getElementById('torontoReviewIndex')) renderIndex(reviews); if(document.getElementById('torontoDetailBody')) renderDetail(reviews); }
  load().catch((err)=>console.error(err));
})();
